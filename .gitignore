# Dependencies
node_modules/

# Next.js build output
.next/
next-env.d.ts

# Environment (contains secrets — never commit)
.env
.env.local
.env*.local

# Local runtime data (customer + order DB, cookie jars)
data/*.db
data/*.db-shm
data/*.db-wal
data/ratelimit.json
cookie.txt
cookies.txt

# Logs / misc
*.log
.DS_Store
check_css.js
